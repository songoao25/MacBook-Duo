# MacBook Duo — Global Overlay Notes (0.6)

全屏覆盖修订：覆盖层改为不抢焦点的 NSPanel，并设置 canJoinAllApplications、canJoinAllSpaces 和 fullScreenAuxiliary，层级提高到 statusBar + 1，目标是支持其他应用的原生全屏空间，并遮住真实 Dock/菜单栏以避免重复。已编译并启动；本次重编译后系统返回 TCC 权限拒绝，全屏场景尚未完成实测。⌘⇧Esc 紧急停止仍保留。

打开 MacBook Duo.app，点击「启用实时桌面效果」。首次需要在系统设置 → 隐私与安全性 → 屏幕与系统音频录制（名称随系统版本略有区别）允许 MacBook Duo。授权后若系统要求退出重开，请照做。

- ⌘⇧G：开启 / 停止全局模式。
- ⌘⇧K：将当前真实铰链角度保存为展开终点。
- ⌘⇧Esc：立即停止并显示设置。
- 菜单栏的双方框图标也可开关、校准、返回设置及退出。
- 截图测试页仍沿用 ⌘H、⌘K、⌘B；这些不是后台全局快捷键。

仅支持 MacBook 内建屏幕。效果在 0° 到保存的终点之间连续展开，达到终点后撤掉覆盖层。开始前请退出旧截图版，避免把旧版全屏窗口当作桌面捕获。

实时版使用 ScreenCaptureKit 排除自身应用并捕获桌面；Metal 在 GPU 上生成玻璃效果，不写入录像文件、不传到网络。当前按屏幕逻辑分辨率捕获 30fps，以控制原型开销。接近终点时撤掉覆盖层，但捕获流仍保持就绪，因此全局模式待机仍有开销；不用时请关闭。

覆盖层不抢键盘焦点，鼠标穿透，系统指针不重复捕获。透视中的视觉按钮位置与真实点击位置存在差异，请在恢复到展开终点后进行精确点击。全屏游戏、Spaces、系统菜单和受保护视频仍需兼容性实测；当前使用 floating 层级，不承诺遮盖系统所有界面。

捕获错误、4 秒无捕获回调、2 秒无有效铰链数据、睡眠或显示器改变时撤掉覆盖层。屏幕切换后不会自动重启，需要手动启用。

修复记录：切换菜单栏/Dock 可见性也会产生显示器参数通知，旧版因此误停止捕获。现在只在内建显示器连接或完整尺寸实际改变时停止。静态桌面不再因缺少后续帧而超时，仅等待首帧设置超时。

验证记录：已无警告编译；在当前机器成功启动实时捕获，8 秒固定角度测试中查看了真实桌面的透视与毛玻璃输出。计数约 29 帧/秒，测试结束后回到原桌面。此计数是捕获帧数，不是端到端显示帧率测试。尚未全面验证所有全屏应用和多 Space 场景。

可点击「先测试 8 秒」或菜单栏「测试实时效果（8秒）」验证渲染，无需移动屏幕。结束后回到实际铰链角度控制；若角度仍低于终点，效果会继续显示。菜单栏状态显示捕获帧数、终点角度，以及当前是否显示效果。

当前源码快照只保留 MacBook Duo 0.6 实现；历史 HingeGlass-v04 目录和 ZIP 不包含在本仓库。全局覆盖层使用独立 Bundle ID，可读取旧版校准值，后续保存互不覆盖。
