# 安全策略

## 支持状态

| 版本 | 状态 |
| --- | --- |
| 0.6 beta | 当前测试版本，接受安全报告 |
| 其他版本 | 不承诺维护 |

## 报告漏洞

请不要公开发布包含可利用细节、桌面截图、日志或个人路径的 Issue。优先使用 GitHub 的 [Private vulnerability reporting](https://github.com/songoao25/MacBook-Duo/security/advisories/new) 私下报告；如果入口不可用，请先创建不含敏感细节的 Issue，说明需要私下沟通。

报告中请尽量包含：

- macOS 版本、Mac 型号和芯片架构；
- 应用版本或源码提交；
- 复现步骤、影响范围和最小复现材料；
- 是否涉及 Screen Recording、HID 传感器、全局覆盖层或安装包。

## 安全边界

- 实时桌面画面由 ScreenCaptureKit 获取并在本机处理；当前源码没有网络上传路径，也不保存录像文件。
- 屏幕录制权限必须由用户在系统设置中授予。权限准备逻辑只处理本应用自己的授权记录，不是授权绕过。
- 分发包是 ad-hoc 开发签名，未公证；请在运行前审阅源码和签名状态。
- 不要把真实桌面截图、权限数据库、凭据或私有日志提交到仓库。
