# 屏幕录制权限

此原型使用临时签名，重编译会改变代码签名身份。为清理不匹配的旧授权，每个构建在当前用户首次启动时执行一次：

`/usr/bin/tccutil reset ScreenCapture studio.prototype.HingeGlass.Global`

只重置本应用的屏幕录制权限，不涉及其他应用、其他权限或铰链校准。成功后记录当前代码签名哈希；同一构建再次启动不会重置。失败不写成功标记，下次启动再试。重置进行中禁止启动捕获。

重置不是自动授权。启用实时桌面效果时仍须用户在系统界面允许屏幕录制，并按系统提示退出重开。当前没有可用的开发者签名证书，此逻辑是开发阶段的迁移处理，不保证消除 macOS 自身的再次确认，也不解决跨构建签名身份不稳定的根本问题。正式发布需要固定签名身份。

验证：构建通过；独立测试覆盖首次运行、记录成功、重新读取持久化状态、新构建变化。实际首次启动显示重置成功，标记与签名哈希一致。
